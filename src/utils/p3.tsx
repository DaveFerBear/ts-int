/**
 * Follow-up items:
 *
 *  - What if /dowork takes a long time to respond (30+ seconds)? How should we handle this?
 *  - What if /dowork is not deterministic or sometimes errors?
 *  - Let's add some features:
 *   - cache expiry/invalidation
 *   - cache serialization (files, redis, LS, etc.)
 *   - analytics/logging
 *
 */
